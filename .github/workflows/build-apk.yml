name: Build Naukari Crown APK

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Restore Naukari Crown logo
        shell: bash
        run: |
          mkdir -p app/src/main/res/drawable-nodpi
          cat assets/logo.part00 assets/logo.part01 assets/logo.part02 assets/logo.part03 assets/logo.part04 | base64 -d > app/src/main/res/drawable-nodpi/naukari_logo.png
          file app/src/main/res/drawable-nodpi/naukari_logo.png
          test -s app/src/main/res/drawable-nodpi/naukari_logo.png

      - name: Install Android SDK 35
        shell: bash
        run: |
          SDKMANAGER="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          if [ ! -x "$SDKMANAGER" ]; then
            SDKMANAGER="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager"
          fi
          yes | "$SDKMANAGER" --licenses >/dev/null || true
          "$SDKMANAGER" "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.7"

      - name: Build debug APK
        run: gradle :app:assembleDebug --stacktrace

      - name: Verify APK
        shell: bash
        run: |
          test -f app/build/outputs/apk/debug/app-debug.apk
          unzip -l app/build/outputs/apk/debug/app-debug.apk | grep -E "classes.dex|resources.arsc|AndroidManifest.xml"
          cp app/build/outputs/apk/debug/app-debug.apk NaukariCrown-v0.3.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: NaukariCrown-v0.3-APK
          path: NaukariCrown-v0.3.apk
          if-no-files-found: error
