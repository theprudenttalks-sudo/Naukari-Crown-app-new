plugins {
    id 'com.android.application'
}

android {
    namespace 'in.naukaricrown.app'
    compileSdk 35

    defaultConfig {
        applicationId "in.naukaricrown.app"
        minSdk 24
        targetSdk 35
        versionCode 7
        versionName "0.2.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
